{
  "name": "simple-app",
  "version": "1.0.0",
  "description": "A simple react app which demonstrates all the basic and the powerfull features without the unwanted dependencies",
  "main": "index.js",
  "dependencies": {
    "react": "^16.3.2",
    "react-dom": "^16.3.2"
  },
  "devDependencies": {
    "browserify": "^16.2.0",
    "watchify": "^3.11.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:js": "browserify src/app.js > dist/bundle.js -d",
    "watch:js": "watchify src/app.js -o dist/bundle.js -dv",
    "watch": "npm run watch:js"
  },
  "keywords": [
    "react",
    "browserify"
  ],
  "author": "sandeepkumaar.c",
  "license": "ISC"
}
